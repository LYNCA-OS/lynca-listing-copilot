name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  offline-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          package-manager-cache: false
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Audit production dependencies
        run: |
          npm ls --omit=dev --all
          node scripts/npm-audit-gate.mjs
      - name: Install PostgreSQL 17 from the official PGDG repository
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ca-certificates curl gnupg
          curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc \
            | sudo gpg --dearmor --yes -o /usr/share/keyrings/postgresql.gpg
          . /etc/os-release
          echo "deb [signed-by=/usr/share/keyrings/postgresql.gpg] https://apt.postgresql.org/pub/repos/apt ${VERSION_CODENAME}-pgdg main" \
            | sudo tee /etc/apt/sources.list.d/pgdg.list >/dev/null
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends postgresql-17
      - name: Put PostgreSQL on PATH
        # The two CSM postgres suites exit 0 when initdb/pg_ctl are missing, so
        # on a runner without them they reported PASS while testing nothing.
        # That is how a persistence defect survived: `buildCsmStageRows` wrote an
        # `evidence` key that `csm_marketplace_structured_check` forbids, which
        # would have failed every atomic write, and the only suite covering it
        # skipped here and could not start a server locally either. A gate that
        # cannot fail is not a gate.
        #
        # Resolve the installed version rather than assuming a runner path, so
        # a runner image bump cannot silently return the suites to skipping.
        run: |
          # 17+ only: csm-product-projection asserts a minimum server version,
          # so exposing an older one turns a skip into a red build without
          # testing anything more than the version check.
          dir=""
          for candidate in $(ls -d /usr/lib/postgresql/*/bin 2>/dev/null | sort -V); do
            major="$(basename "$(dirname "$candidate")")"
            if [ "${major%%.*}" -ge 17 ] 2>/dev/null; then dir="$candidate"; fi
          done
          if [ -n "$dir" ]; then
            echo "$dir" >> "$GITHUB_PATH"
            echo "PostgreSQL on PATH: $dir"
          else
            echo "::error::PostgreSQL 17+ is required; refusing to let persistence suites skip"
            exit 1
          fi

      - name: Check the production import graph and contracts
        run: npm run check
      - name: Run the retained thin-path and production suites
        run: npm run test:csm-thin && npm run test:production && npm run test:accuracy
      - name: Syntax-check API handlers and app bundle
        run: |
          set -e
          for f in api/*.js api/v4/*.js app/*.js; do
            node --check "$f"
          done
