// Compatibility re-export. The module moved to `csm/ontology/` under COS-42's
// ownership convergence; this shim keeps every existing import working so the
// directory move changes no behaviour, which is the rule for a
// directory-only change.
export * from "../../../csm/ontology/product-semantics.mjs";
