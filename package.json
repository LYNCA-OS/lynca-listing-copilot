{
  "name": "lynca-listing-copilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Metaverse Listing Copilot internal title generation webtool.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "start": "node scripts/dev-server.mjs",
    "check": "node --check app/listing-copilot.js && node --check app/login.js && node --check api/login.js && node --check api/logout.js && node --check api/session.js && node --check api/listing-copilot-title.js && node --check scripts/dev-server.mjs && node --check middleware.js && node scripts/listing-confidence-audit.test.mjs",
    "test:mock": "node scripts/listing-confidence-audit.test.mjs"
  },
  "dependencies": {
    "@vercel/functions": "latest"
  }
}
